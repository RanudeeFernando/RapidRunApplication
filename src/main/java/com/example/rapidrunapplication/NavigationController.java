package com.example.rapidrunapplication;

import javafx.event.ActionEvent;

// An interface for controllers that handle navigation
public interface NavigationController {
    // Handles the action to navigate to the main screen
    void handleGoToMain(ActionEvent event);
}
